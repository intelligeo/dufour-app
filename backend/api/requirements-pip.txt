# Dufour Middleware API - Python Dependencies (pip only)
# fiona, shapely, pyproj are installed via apt in the Dockerfile
# to guarantee GDAL version compatibility with the QGIS Server base image.

# Web Framework
fastapi==0.103.2
uvicorn[standard]==0.24.0
python-multipart==0.0.9
httpx==0.26.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
asyncpg==0.29.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5
pytest-mock==3.12.0

# XML/JSON
lxml==5.1.0
pydantic==2.5.3

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1

# Image processing
Pillow==10.2.0
